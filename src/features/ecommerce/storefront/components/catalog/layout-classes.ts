/** Breaks out of storefront main horizontal padding without causing viewport overflow. */
export const STOREFRONT_MAIN_FULL_BLEED_CLASS =
  '-mx-4 w-[calc(100%+2rem)] max-w-none sm:-mx-6 sm:w-[calc(100%+3rem)]' as const;
